/**
 * Core Logic for Entorhino Frontend
 * Speed-optimized, minimal JS overhead.
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log('Entorhino Modern Frontend Initialized');
    // Global micro-animations, glassmorphism enhancements, and fast routing logic here.
});

// Sidebar Toggle Functionality (for mobile responsiveness)
function toggleSidebar() {
    const sidebar = document.getElementById('sidebar');
    if (sidebar) {
        sidebar.classList.toggle('active');
    }
}
